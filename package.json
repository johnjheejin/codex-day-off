{
  "name": "afterglow",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/afterglow-three.js src/afterglow-garden.js --bundle --minify --format=iife --target=es2020 --legal-comments=linked --outdir=assets",
    "dev": "python3 -m http.server 8000",
    "test": "node scripts/check-runtime-guards.mjs && node --test tests/*.test.js && playwright test"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "esbuild": "0.28.2"
  }
}
